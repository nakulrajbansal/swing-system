"""Desktop GUI app: configure API keys + run parameters, run the system, see output.

Paper-only by default. Live broker and real-LLM paths remain gated (master
invariants 3 & 5). The one-click runs exercise the deterministic, offline
pipeline (validation harness + paper-trading engine).
"""

APP_NAME = "Swing System"
APP_VERSION = "0.1.0"
