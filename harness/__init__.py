"""AI-native swing trading system — validation harness.

Phase 1, build step 1: the point-in-time data store and corporate-action
adjustment. Nothing here calls the network, an LLM, or an agent (harness spec
section 9). The integrity foundation everything downstream depends on.
"""
